"use client";
import Link from "next/link";
import {Menu,X} from "lucide-react";
import {useState} from "react";

const links=[["Assessment","/predict"],["Dashboard","/dashboard"],["Reports","/reports"]];

export function Nav(){const [open,setOpen]=useState(false);return <nav className="sticky top-0 z-50 border-b border-white/[.07] bg-[#080b12e8] backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 md:px-6"><Link href="/" className="flex min-w-0 items-center gap-3 font-extrabold" onClick={()=>setOpen(false)}><span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg border border-cyan/40 bg-cyan/10 text-xs text-cyan">LG</span><span className="truncate">LendGuard</span></Link><div className="hidden items-center gap-7 text-sm text-slate-400 md:flex">{links.map(([label,href])=><Link key={href} className="hover:text-white" href={href}>{label}</Link>)}</div><div className="flex items-center gap-2"><Link className="rounded-lg border border-white/15 px-3 py-2 text-sm font-semibold hover:border-cyan/50 sm:px-4" href="/login" onClick={()=>setOpen(false)}>Sign in</Link><button className="grid h-10 w-10 place-items-center rounded-lg border border-white/15 text-slate-300 hover:border-cyan/50 hover:text-white md:hidden" type="button" aria-label={open?"Close navigation":"Open navigation"} title={open?"Close navigation":"Open navigation"} onClick={()=>setOpen(!open)}>{open?<X size={19}/>:<Menu size={19}/>}</button></div></div>{open&&<div className="border-t border-white/[.07] px-5 py-3 md:hidden">{links.map(([label,href])=><Link key={href} className="block rounded-lg px-3 py-3 text-sm font-semibold text-slate-300 hover:bg-white/[.06] hover:text-white" href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</div>}</nav>}
