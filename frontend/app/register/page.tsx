"use client";import {FormEvent,useState} from "react";import {api} from "@/lib/api";import {useRouter} from "next/navigation";
export default function Register(){const r=useRouter(),[error,setError]=useState("");async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const d=await api("/api/v1/auth/register",{method:"POST",body:JSON.stringify(Object.fromEntries(f))});localStorage.setItem("token",d.access_token);r.push("/dashboard")}catch(x:any){setError(x.message)}}return <main className="mx-auto max-w-md px-6 py-16"><form onSubmit={go} className="glass rounded-3xl p-8"><h1 className="text-3xl font-black">Create workspace</h1><p className="mb-6 mt-2 text-slate-400">Start making clearer lending decisions.</p><input name="full_name" className="field mb-3" placeholder="Full name" required/><input name="email" type="email" className="field mb-3" placeholder="Email" required/><input name="password" type="password" minLength={8} className="field mb-3" placeholder="Password (8+ characters)" required/>{error&&<p className="mb-3 text-red-400">{error}</p>}<button className="button w-full">Create account</button></form></main>}
