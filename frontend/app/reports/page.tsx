"use client";import Link from "next/link";import {useState} from "react";import {API} from "@/lib/api";
export default function Page(){const [error,setError]=useState("");async function download(path:string){setError("");const token=localStorage.getItem("token");if(!token){setError("Sign in to download reports.");return}const r=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${token}`}});if(!r.ok){const data=await r.json().catch(()=>({}));setError(data.detail||"Report download failed");return}const b=await r.blob();const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=path.endsWith('pdf')?'loan-risk-report.pdf':'loan-risk-report.xlsx';a.click();URL.revokeObjectURL(a.href)}return <main className="mx-auto max-w-4xl px-6 py-20"><p className="text-cyan">REPORTING</p><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><h1 className="text-5xl font-black">Decision-ready exports.</h1>{error&&<Link href="/login" className="rounded-xl border border-cyan/30 px-4 py-2 text-sm font-semibold text-cyan hover:bg-cyan/10">{error}</Link>}</div><div className="mt-10 grid gap-5 md:grid-cols-2"><button onClick={()=>download('/api/v1/reports/pdf')} className="glass rounded-2xl p-8 text-left"><b className="text-2xl">PDF risk report</b><p className="mt-2 text-slate-400">A clean, printable decision summary.</p></button><button onClick={()=>download('/api/v1/reports/excel')} className="glass rounded-2xl p-8 text-left"><b className="text-2xl">Excel data export</b><p className="mt-2 text-slate-400">Prediction history for deeper analysis.</p></button></div></main>}
