"use client"; import Link from "next/link";import {FormEvent,useState} from "react";import {API} from "@/lib/api";import {useRouter} from "next/navigation";
export default function Login(){const r=useRouter();const [error,setError]=useState("");async function go(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body=new URLSearchParams({username:String(f.get("email")),password:String(f.get("password"))});const res=await fetch(`${API}/api/v1/auth/login`,{method:"POST",body});const data=await res.json();if(!res.ok)return setError(data.detail);localStorage.setItem("token",data.access_token);r.push("/dashboard")}return <main className="mx-auto max-w-md px-6 py-20"><form onSubmit={go} className="glass rounded-3xl p-8"><h1 className="text-3xl font-black">Welcome back</h1><p className="mb-7 mt-2 text-slate-400">Access your lending intelligence workspace.</p><input name="email" type="email" placeholder="Email" className="field mb-4" required/><input name="password" type="password" placeholder="Password" className="field mb-4" required/>{error&&<p className="mb-3 text-red-400">{error}</p>}<button className="button w-full">Sign in</button><p className="mt-5 text-center text-sm text-slate-500">New here? <Link className="text-cyan" href="/register">Create account</Link></p></form></main>}
